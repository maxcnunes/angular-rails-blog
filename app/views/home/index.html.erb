<div ng-controller="PostsCtrl" id="posts-ctrl">
  <div id="post-list-container">
    <ul>
      <li ng-repeat="post in posts">
        <h3>{{post.title}} <small>({{post.published}})</small></h3>
      </li>
    </ul>
  </div>
</div>